"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var GetInstanceFieldHelper_exports = {};
__export(GetInstanceFieldHelper_exports, {
  GetInstanceFieldHelper: () => GetInstanceFieldHelper
});
module.exports = __toCommonJS(GetInstanceFieldHelper_exports);
var import_Command = require("../../utils/Command.cjs");
var import_CommandType = require("../../utils/CommandType.cjs");
class GetInstanceFieldHelper {
  /**
   * @param {Command | null | undefined} command
   * @param {string} fieldName
   * @returns {any}
   */
  static getFieldValue(command, fieldName) {
    if (!command || !command.payload || command.payload.length === 0) {
      return null;
    }
    if (command.commandType === import_CommandType.CommandType.SetInstanceField && command.payload.length >= 3) {
      const name = GetInstanceFieldHelper._getPayloadValue(command.payload[1]);
      if (typeof name === "string" && name === fieldName) {
        return GetInstanceFieldHelper._getPayloadValue(command.payload[2]);
      }
    }
    if (command.commandType === import_CommandType.CommandType.Array) {
      for (let i = 0; i < command.payload.length; i++) {
        const element = command.payload[i];
        if (element instanceof import_Command.Command) {
          const result = GetInstanceFieldHelper.getFieldValue(element, fieldName);
          if (result !== null) {
            return result;
          }
        }
      }
      return null;
    }
    const firstPayload = command.payload[0];
    return firstPayload instanceof import_Command.Command ? GetInstanceFieldHelper.getFieldValue(firstPayload, fieldName) : null;
  }
  /**
   * @param {Command | null} command
   * @returns {string | null}
   * @throws {Error} if command is null or undefined
   */
  static getResultType(command) {
    if (!command) {
      throw new Error("GetInstanceFieldHelper.getResultType: command must not be null or undefined");
    }
    if (!command.payload || command.payload.length === 0) {
      return null;
    }
    if (command.commandType === import_CommandType.CommandType.GetResultType || command.commandType === import_CommandType.CommandType.GetType) {
      return GetInstanceFieldHelper._getPayloadValue(command.payload[0]);
    }
    if (command.commandType === import_CommandType.CommandType.Array) {
      for (let i = 0; i < command.payload.length; i++) {
        const element = command.payload[i];
        if (element instanceof import_Command.Command) {
          const result = GetInstanceFieldHelper.getResultType(element);
          if (result !== null) {
            return result;
          }
        }
      }
      return null;
    }
    const firstPayload = command.payload[0];
    return firstPayload instanceof import_Command.Command ? GetInstanceFieldHelper.getResultType(firstPayload) : null;
  }
  /**
   * @param {any} payloadItem
   * @returns {any}
   */
  static _getPayloadValue(payloadItem) {
    if (payloadItem instanceof import_Command.Command && payloadItem.commandType === import_CommandType.CommandType.Value && payloadItem.payload.length > 0) {
      return payloadItem.payload[0];
    }
    return payloadItem;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  GetInstanceFieldHelper
});
